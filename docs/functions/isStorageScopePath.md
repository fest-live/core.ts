[**@fest-lib/core v0.1.27**](../README.md)

***

[@fest-lib/core](../README.md) / isStorageScopePath

# Function: isStorageScopePath()

```ts
function isStorageScopePath(input): boolean;
```

Defined in: UserPath.ts:53

`/user/` and `/idb/` are the same virtual-storage shape; only the backend differs.

## Parameters

### input

`any`

## Returns

`boolean`
